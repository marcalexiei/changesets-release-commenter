---
'changesets-release-commenter': minor
---

Credit the action in a footer at the end of each comment. It is placed before the hidden marker, so a thread already carrying a comment from an earlier version still deduplicates, and `footer: false` leaves it out.
